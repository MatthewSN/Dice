const initialState = {
  currentPoint: 0,
  totalPoints: 0,
  rank: 0,
  record: 0,
  pointInRecord: 0
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
