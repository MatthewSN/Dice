const initialState = {
  name: "",
  point: 0,
  rank: 0,
  record: 0,
  poinInRecord: 0,
  image: "",
  token: ""
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
